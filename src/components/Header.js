import React from 'react';
import {
    HeaderContainer,
    AnimatedText
} from '../styles/GlobalStyles.styles';

const Header = () => {
    return (
        <HeaderContainer>
            <AnimatedText>Unify</AnimatedText>
        </HeaderContainer>
    );
};

export default Header;
